{
  "name": "zk",
  "version": "1.0.0",
  "description": "ZK application",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.18.0"
  },
  "scripts": {
    "setup": "./setup.sh",
    "dev": "pnpm --filter app dev",
    "build:circuits": "pnpm --filter circuits build",
    "build:app": "pnpm --filter app build",
    "build:contracts": "cd contracts && cargo build --release",
    "deploy:contracts": "cd contracts && source .env && cargo stylus deploy --endpoint=$RPC_URL --private-key=$PRIVATE_KEY --constructor-args $OWNER_ADDRESS"
  },
  "workspaces": [
    "circuits",
    "app"
  ],
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "keywords": [
    "zk-proofs",
    "arbitrum",
    "stylus",
    "groth16",
    "circom",
    "snarkjs",
    "privacy",
    "zero-knowledge"
  ],
  "author": "Thiago Rocha",
  "license": "MIT"
}